@model IEnumerable<Movie.Models.Film>

@{
    ViewBag.Title = "getAllFilm";
}

<h2>getAllFilm</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.idFilm)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.nameFilm)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.duration)
        </th>
        <th></th>
    </tr>

    @foreach (var item in Model)
    {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.idFilm)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.nameFilm)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.duration)
            </td>
            <td>
                @Html.ActionLink("Đặt vé", "bookShowTimeByIdFilm", new { idFilm = (int)item.idFilm })
            </td>
        </tr>
    }

</table>
